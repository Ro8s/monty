#include "monty.h"
/**
 * all - prints all the values on the stack from the top
 * @list: header of the list.
 * @line: line of the command.
 * ---------------------
 */
void all(stack_t **list, unsigned int line)
{
	stack_t *aux = *list;

	





}
